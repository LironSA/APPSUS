'use strict';

export default {
    template: `
                <section class="email-filter">
                <input type="text" placeholder="search email">    
                <select></select>
                </section>

    `
}